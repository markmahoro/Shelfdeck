{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "helix://contracts/capabilities/libra.external_material.acquire.observe/v1/parameters",
  "title": "libra.external_material.acquire.observe@1 parameters",
  "type": "object",
  "additionalProperties": false,
  "properties": {
    "phase": {
      "type": "string",
      "enum": [
        "download",
        "transfer"
      ]
    }
  },
  "required": [
    "phase"
  ]
}
