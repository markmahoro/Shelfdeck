# Emby Desktop Player — 开发计划

> **文档性质**：**项目管理与里程碑**（范围、阶段排序、完成度快照、参考索引）。  
> **不含**转码/任务 Flow 的技术实现细则——`**transcode` 及任务调度相关实现以 `TASK_CENTER_FULL_LOGIC.md`（尤其 §2.4、§17）为单一事实来源（SSOT）**。  
> **生成日期**：2026-04-18（+08:00）；**当前里程碑**：`v1.0.0-beta.8`（删除任务 Flow、Emby 真删、用户访问令牌鉴权）

---

## 1. 项目概况

**Emby Desktop Player** 目标是在 Windows 上提供：

- **前台**：未播放 → 第三方播放器 → 回写已播放的闭环。
- **后台**：按星级策略的媒体库治理（转码 / 补源）与可恢复、可审计的任务调度。

**默认工作目录**：以本机克隆的仓库根为准（示例：`E:\my_project\emby_third_party`）；勿与旧路径多副本混用，避免配置与 `localStorage` 不一致。

---

## 2. 已实现（可在代码中对照验证）


| 领域            | 内容（摘要）                                                                                       |
| ------------- | -------------------------------------------------------------------------------------------- |
| **文档**        | PRD v1.0.0；**任务中心 / 转码实现 SSOT**：`TASK_CENTER_FULL_LOGIC.md`；进度时间线：`DEVELOPMENT_PROGRESS.md`。 |
| **壳层与导航**     | 五页：配置、海报墙、媒体库、任务中心、播放记录；`mvp/src/App.tsx` 等。                                                 |
| **任务中心（前端）**  | `taskQueue.ts`、删除 Flow 与 Emby 真删（beta.8）、三类型/三队列模拟、软停与批量操作等。                                 |
| **配置 / 媒体策略** | 配置中心表单；`mediaManager.ts` 等（详见 PRD §4～§5）。                                                    |
| **工程**        | Electron + React（Vite）；`mvp/electron/main.js`、`preload.js`、Emby/豆瓣 IPC。                      |


---

## 3. 未实现或仅占位（相对正式版）


| 领域                                | 说明                                                                  |
| --------------------------------- | ------------------------------------------------------------------- |
| **真实后台执行**                        | FFmpeg 转码、临时文件与原子替换、MoviePilot 等：**细则见 SSOT §2.4**；当前任务生命周期多在渲染层模拟。 |
| **进程分层**                          | main 调度 + worker 执行：规划中；调度核心仍在 `taskScheduler.ts`（渲染层）。             |
| **托盘 / checkpoint / 批量预估 / 观影降载** | 见阶段 C 与 PRD §8～§9。                                                  |


---

## 4. 下一步开发计划（按依赖与风险排序）

> 每条仅保留 **交付目标**；**设计/行为** 以 `TASK_CENTER_FULL_LOGIC.md`、`EmbyDesktopPlayer_PRD_v1.0.0_modules.md` 为准。

### 阶段 A — 主进程与真实 Emby（前台闭环）

1. 维护 Emby REST：连接、列表、路径解析、播放回写与版本兼容。
2. `preload` IPC 与可选调试桩。
3. 真实 `launchPlayer`：映射、参数、外部播放器、会话与阈值。

### 阶段 B — 后台任务真执行（最小可用）

1. **转码**：worker/子进程接入 FFmpeg；端到端按 `**TASK_CENTER_FULL_LOGIC.md` §2.4**（预检 → 槽与资源池 → partial → 校验 → replace → 清理）；异常 **§17**；暂停 **§9.7 / §2.4.11**。任务卡体积字段等见 **§2.4.9**。
2. **MoviePilot**：HTTP 客户端与配置；搜索 → 候选 → 确认；排序与自动化迭代。
3. **调度迁移**：队列与状态逐步迁入 main + IPC，渲染层仅展示与控制。

### 阶段 C — 体验与可靠性（正式版必备）

1. 托盘与关窗行为（PRD §8.3）。
2. checkpoint 与中断恢复（与 SSOT 步骤绑定）。
3. 批量执行前粗估 UI（PRD §7.4）。
4. 任务日志落盘与检索。
5. 渲染态 →主进程 `userData` 持久化迁移（减少 `localStorage` 依赖）。

### 并行维护

- 每完成可演示里程碑，更新 `**DEVELOPMENT_PROGRESS.md`**。
- 迭代基线以当前主分支合并结果为准。

---

## 5. 后续 Idea（项目管理备忘，未排期）

> 技术方案若与 **转码/任务**相关，以 `**TASK_CENTER_FULL_LOGIC.md`** 后续修订为准；本节仅作 **立项前占位**。

- **应用内播放（mpv / libmpv）**：未排期；见历史 PRD Idea 与调研结论。
- **单条媒体自定义转码参数**：未排期；数据模型与 UI 待立项。
- **远端编码节点 / 资源池横向扩容**：设计备忘见 **SSOT §2.4.10**；本期不做。

---

## 6. 参考文件


| 文件                                        | 用途                                                     |
| ----------------------------------------- | ------------------------------------------------------ |
| `TASK_CENTER_FULL_LOGIC.md`               | **任务中心全量 SSOT**（含 §2.3～§2.5、§6～§18、跨页流程 **§19**；`transcode` 见 §2.4、§17；§9.7 转码暂停例外） |
| `DEVELOPMENT_PROGRESS.md`                 | 仓库进度与时间线 SSOT                                          |
| `EmbyDesktopPlayer_PRD_v1.0.0_modules.md` | 产品能力定义（按模块）                                            |
| `mvp/`                                    | 可运行 MVP 工程                                             |
