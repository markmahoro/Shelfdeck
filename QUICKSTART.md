# Quick Start

## 5 分钟上手 ShelfDeck

### 第一步：启动

解压 `ShelfDeck-v1.0.0.zip`，双击 `shelfdeck_service启动器.vbs`。

→ 托盘图标出现 + 浏览器打开管理页面 `http://127.0.0.1:18080/admin`

### 第二步：添加媒体库

管理页面 → 仪表盘 → **添加媒体库**：

1. **登录 Emby**：填入 Emby 服务器地址（如 `http://192.168.1.100:8096`）、用户名、密码 → 点"登录 Emby"
2. **选择文件夹**：选择要同步的媒体文件夹（如"电影"）
3. **配置**：
   - 名称：默认用文件夹名
   - **路径映射**（重要）：如果 ShelfDeck 和 Emby 在不同机器上访问文件，需要配。如 Emby 路径是 `/volume1/Media`，本地路径是 `Z:\`，填 `/volume1/Media` → `Z:\`
   - 豆瓣同步：可选启用
   - 策略模板：默认策略即可
4. 点"完成添加"

→ 等待几秒，媒体库数据加载完毕，策略推荐自动生成。

### 第三步：查看策略

管理页面 → **媒体库管理**：

- 每行显示影片的码率、体积、评分、策略推荐
- 策略列显示"需转码"、"建议洗版"、"低分删除"、"策略未覆盖"等
- 可选筛选/排序

### 第四步：执行任务

选一条推荐"需转码"或"建议洗版"的影片 → 点右侧"入队"按钮 → 任务进入调度。

管理页面 → **任务中心** 查看进度：
- 任务自动执行（`full_auto` 模式）或点击"继续"手动触发
- 转码：FFmpeg 编码 → 验证 → 替换
- 洗版：MoviePilot 搜索 → 选种 → 下载 → 刮削 → 替换

### 额外

- **桌面客户端**：双击 `ShelfDeck播放助手.exe`，浏览未观看内容、管理媒体库
- **问题排查**：管理页面仪表盘右下角"查看完整运行日志"
- **智能入队**：在"系统设置"配置后，看过+打分的内容会自动生成任务

## 常见场景

### 只有 Emby，没有 MoviePilot

可以正常使用删除和转码功能。洗版功能需要 [MoviePilot](https://github.com/jxxghp/MoviePilot) 服务。

### ShelfDeck 和 Emby 在同一台机器

不需要配路径映射——Emby 返回的本地路径 ShelfDeck 可以直接访问。

### ShelfDeck 在 Windows，Emby 在 NAS

需要在添加媒体库时配置路径映射。如 NAS 上 Emby 看到的路径是 `/volume1/Media`，Windows 映射到 `Z:\`，则填 `/volume1/Media` → `Z:\`。
