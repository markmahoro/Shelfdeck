# ShelfDeck 一键启动/关闭脚本使用说明

## 文件位置

所有脚本都在项目根目录：`E:\my_project\emby_third_party\`

- `start-shelfdeck.bat` - 一键启动脚本
- `stop-shelfdeck.bat` - 一键关闭脚本
- `create-shortcuts.bat` - 创建桌面快捷方式的脚本

## 方法一：直接双击运行（推荐）

1. 打开文件资源管理器，进入 `E:\my_project\emby_third_party\`
2. 双击 `start-shelfdeck.bat` 启动所有服务
3. 双击 `stop-shelfdeck.bat` 关闭所有服务

## 方法二：创建桌面快捷方式

### 手动创建快捷方式：

1. 在桌面空白处右键 → 新建 → 快捷方式
2. 输入位置：`E:\my_project\emby_third_party\start-shelfdeck.bat`
3. 命名为：`启动 ShelfDeck`
4. 完成

重复以上步骤创建关闭快捷方式：
- 位置：`E:\my_project\emby_third_party\stop-shelfdeck.bat`
- 名称：`关闭 ShelfDeck`

### 或者运行自动创建脚本：

1. 进入 `E:\my_project\emby_third_party\`
2. 双击 `create-shortcuts.bat`
3. 桌面上会自动生成两个快捷方式

## 启动后会看到什么

启动脚本会依次打开 3 个命令行窗口：
1. **ShelfDeck-MediaService** - 后端服务（端口 18080）
2. **ShelfDeck-Tray** - 系统托盘小助手
3. **ShelfDeck-Desktop** - 桌面客户端（开发模式）

## 关闭说明

关闭脚本会自动终止所有相关进程，包括：
- Node.js 进程（media-service 和 tray-supervisor）
- Electron 进程（media-desktop）

## 注意事项

- 启动脚本会按顺序启动服务，每个服务之间有延迟确保正常启动
- 关闭脚本会强制终止进程，请确保已保存所有工作
- 如果某个服务启动失败，检查对应的命令行窗口查看错误信息
